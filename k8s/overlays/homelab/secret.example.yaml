# Copy to secret.yaml (gitignored if you want) or create with:
#   kubectl -n chrome-cell create secret generic chrome-cell-ui \
#     --from-literal=PASSWORD='change-me'
apiVersion: v1
kind: Secret
metadata:
  name: chrome-cell-ui
  labels:
    app.kubernetes.io/name: chrome-cell
type: Opaque
stringData:
  PASSWORD: change-me
