apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: chrome-cell
spec:
  template:
    spec:
      containers:
        - name: chrome
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
