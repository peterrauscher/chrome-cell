apiVersion: v1
kind: Service
metadata:
  name: viewport-gateway-ui
  labels:
    app.kubernetes.io/name: viewport-gateway
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: viewport-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
